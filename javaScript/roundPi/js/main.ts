"use strict";

// crear numero aleatorio con Math
const randomNumber: number = Math.round(Math.random());
console.log(randomNumber);

// redondear pi
const roundPi = Math.round(Math.PI);
console.log(roundPi);
