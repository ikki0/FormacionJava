"use strict";
var arrayLetters = ['a', 'b', 'c', 'd', 'e'];
arrayLetters.forEach(function (element) {
    console.log(element);
});
var number = 5;
for (var i = number; i >= 0; i--) {
    console.log(number);
    number--;
}
"patata".replaceAll('a', 'e');
