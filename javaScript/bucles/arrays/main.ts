"use strict";
const arrayLetters : string[] = ['a', 'b', 'c' , 'd', 'e'];

arrayLetters.forEach(element => {
    console.log(element);
});

let number = 5;

for (let i = number ; i >= 0 ; i--){
    console.log(number);
    number--;
}


"patata".replaceAll('a','e');